description: Infineon MCMCAN node driver 

compatible: "infineon,mcmcan-node"

include: ["bosch,m_can-base.yaml", "pinctrl-device.yaml"]

properties:
  reg:
    required: true
  reg-names:
    required: true
  interrupts:
    required: true
  interrupt-names:
    required: true
